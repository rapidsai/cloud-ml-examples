name: cuML RF test

conda_env: envs/conda.yaml

entry_points:
    main:
        parameters:
            fpath: {type: str}
            mode: {type: str, default: 'GPU'}
            algo: {type: str, default: 'tpe'}
        command: "python src/rf_test/train.py --fpath={fpath} --mode={mode} --algo={algo}"
